# Database (used when running backend outside Docker)
DATABASE_URL=postgresql://skillgap:skillgap_dev@localhost:5432/skillgap

# Optional: enable LLM mode for richer suggestions (only if key is set)
OPENAI_API_KEY=
USE_LLM_MODE=false

# CORS (comma-separated origins; * for dev)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
